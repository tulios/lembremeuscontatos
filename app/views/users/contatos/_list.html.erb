<% if @contatos and (not @contatos.empty?) %>
	<%= render :partial => 'users/contatos/contato', :collection => @contatos %>
	<%= will_paginate @contatos %>
<% else %>
	<em><%= t "app.contato.nenhum_encontrado" %></em>
<% end %>
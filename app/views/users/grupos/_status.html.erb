<div class="status">
	<span class="label"><%= t ("activerecord.attributes.grupo.status") %>:</span>
	<br/>
	<%= @grupo.status_str %>      
	<% if @grupo.inativo? %>
		<span class="divisor">&raquo;</span>
		<span class="ativar">
			<a id="ativar" href="#form_ativar"><%= t("app.grupo.ativar") %></a>
		</span>
	<% end %>
</div>
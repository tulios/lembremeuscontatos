<% if @grupos and (not @grupos.empty?) %>
	<%= render :partial => 'users/grupos/grupo', :collection => @grupos %>
	<%= will_paginate @grupo %>
<% else %>
	<em><%= t "app.grupo.nenhum_encontrado" %></em>
<% end %>